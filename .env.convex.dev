# Convex deployment environment variables (dev)
# Sync to deployment: npx convex env set --from-file .env.convex.dev
# WARNING: This file is tracked in git. Never commit secrets here.
# Set secrets (BETTER_AUTH_SECRET, E2E_CLEANUP_SECRET, etc.) via the CLI
# or keep them in untracked .env.local for local dev.
SITE_URL=https://exam-prep.localhost
